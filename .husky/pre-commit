#!/bin/sh

# Lints the staged JavaScript and runs the unit tests that import it (the
# lint-staged section of package.json). The hook used to run npm audit, which
# needs the network, and all of Vitest on every commit; the Tests and
# Dependency Review workflows run both on every pull request.
npx lint-staged
